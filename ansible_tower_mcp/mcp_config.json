{
  "mcpServers": {
    "ansible": {
      "command": "ansible-mcp",
      "args": ["--transport", "stdio"],
      "env": {
        "ANSIBLE_URL": "${ANSIBLE_URL:-http://localhost:8080}",
        "ANSIBLE_TOKEN": "${ANSIBLE_TOKEN}"
      }
    }
  }
}
